# Use a minimal base image
FROM alpine:latest

# Set the working directory
WORKDIR .

# Copy nothing for now, just to have a valid Dockerfile
# COPY . .

# Keep the container running (optional, for testing purposes)
CMD ["sleep", "infinity"]
